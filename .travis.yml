language: ruby
rvm:
  - 2.1.5
  - 1.9.3
  - jruby-19mode
  - rbx-2.1.1
before_script:
  - cp config/database.example.yml config/database.yml
  - cp config/application.example.yml config/application.yml
  - bin/rake db:create db:migrate
after_script:
  - bin/rake rubocop app spec lib
